{
	"private": true,
	"scripts": {
		"build": "mdbook build",
		"serve": "mdbook serve",
		"format": "prettier --cache --write .",
		"preinstall": "npx only-allow pnpm",
		"prepare": "husky",
		"update": "pnpm up && pnpm dedupe"
	},
	"license": "CC-BY-NC-SA-4.0",
	"devDependencies": {
		"@commitlint/cli": "^20.3.1",
		"@commitlint/config-conventional": "^20.3.1",
		"husky": "^9.1.7",
		"lint-staged": "^16.2.7",
		"only-allow": "^1.2.2",
		"prettier": "^3.8.1"
	},
	"lint-staged": {
		"*": "prettier --ignore-unknown --write"
	}
}
